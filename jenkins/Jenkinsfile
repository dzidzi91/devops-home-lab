pipeline {
    agent any

    environment {
        REGISTRY_IMAGE = "localhost:5000/devops-platform-app"
        IMAGE_TAG = "ci"
        APP_CONTAINER = "devops-platform-app"
        APP_PORT = "8000"
    }

    stages {
        stage('Pull Image') {
            steps {
                echo "Pulling image ${REGISTRY_IMAGE}:${IMAGE_TAG}..."
                sh "docker pull ${REGISTRY_IMAGE}:${IMAGE_TAG}"
            }
        }

        stage('Stop Old Container') {
            steps {
                echo "Stopping old container if it exists..."
                sh '''
                    docker stop ${APP_CONTAINER} || true
                    docker rm ${APP_CONTAINER} || true
                '''
            }
        }

        stage('Run New Container') {
            steps {
                echo "Starting new container from ${REGISTRY_IMAGE}:${IMAGE_TAG}..."
                sh '''
                    docker run -d \
                      --name ${APP_CONTAINER} \
                      -p ${APP_PORT}:8000 \
                      ${REGISTRY_IMAGE}:${IMAGE_TAG}
                '''
            }
        }

        stage('Health Check') {
            steps {
                echo 'Waiting for application to start...'
                sh '''
                    for i in $(seq 1 15); do
                      curl -fsS http://localhost:${APP_PORT} && exit 0
                      sleep 2
                    done
                    echo "Application health check failed."
                    exit 1
                '''
            }
        }
    }

    post {
        success {
            echo "Deployment successful: ${REGISTRY_IMAGE}:${IMAGE_TAG}"
        }
        failure {
            echo "Deployment failed."
            sh 'docker ps -a || true'
        }
    }
}